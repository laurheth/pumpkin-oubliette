/** Art for a square or object */
export interface Art {
    art?: string;
    foreground?: string;
    background?: string;
}

/** Coordinates on the map */
export interface Position {
    x:number;
    y:number;
}